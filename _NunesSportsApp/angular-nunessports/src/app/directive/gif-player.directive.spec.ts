import { GifPlayerDirective } from './gif-player.directive';

describe('GifPlayerDirective', () => {
  it('should create an instance', () => {
    const directive = new GifPlayerDirective();
    expect(directive).toBeTruthy();
  });
});
